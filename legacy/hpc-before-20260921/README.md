# Historical HPC files

These are exact copies of the configurations and scripts saved in commit 351cfe1
before merging the new workflow on 2026-09-21. They use the retired configuration
schema and are retained for reference only. Do not submit these jobs or run the
old dataset helper with the current code.

The active configuration is `configs/hpc.toml`; see `HPC.md` at the repository root.
